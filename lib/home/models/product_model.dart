class ProductModel {
    int id;
    String imageUrl;
    String name;
    String brand;
    double price;
    int count;
    ProductModel(this.id, this.imageUrl, this.name, this.brand, this.price, this.count);
}